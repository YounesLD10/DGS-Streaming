# Terraform — local state, providers, plans
**/.terraform/
**/.terraform.tfstate.lock.info
*.tfstate
*.tfstate.*
*.tfplan
tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Generated by setup_poc.sh — regenerate with `./setup_poc.sh up`
terraform/*.tf
terraform/stage2/*.tf
k8s/
scripts/

# Local Fernet key (generated once, NEVER commit)
.fernet.key

# Local CSV data (PAN/PII — NEVER commit)
data/

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/
.claude/
*.swp
*.swo
.producer-venv/
