# HPS Real-Time Pipeline — Python dependencies
# Install order matters: apache-flink first so its vendored JARs
# are in place before the connector JARs are downloaded in the Dockerfile.

apache-flink==1.19.1
minio>=7.2.0
cryptography>=41.0.0
